using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EventManager : MonoBehaviour
{
    //This script will be incharge of managing random horror events during the day
    //Each time a player washes their face, the insane meter goes up
    //different events have a chance of triggering based on what the insane meter is at

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
