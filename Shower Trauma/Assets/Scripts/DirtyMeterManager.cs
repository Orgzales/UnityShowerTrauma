using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DirtyMeterManager : MonoBehaviour
{
    //This script will be incharge of managing the dirty meter of the player
    //When meter is 0, the player will end the day
    //When player is in the shower the meter will constantly decrease
    //When player is washing face, the meter will jump down large amount


    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
